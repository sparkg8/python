"""
MODULES:
    There are many built-in Python modules named: [math, sys, os, statistics, collections, random]
    Also user can create it's own modules and import them.

    -> To use a module file you just type import <module_name>

    import <module_name> ### import the module if there is stored on the same working directory
    import <module_name> as myMod ### this you can specify how to call your module in the code
    from <modules_path> import <module_name> ### when you have stored your module in different directory
    
    """
